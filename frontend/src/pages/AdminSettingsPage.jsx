import React from 'react';

const AdminSettingsPage = () => (
  <div className="page-container">
    <h2>Settings (Admin)</h2>
    <p>Site configuration and settings.</p>
  </div>
);

export default AdminSettingsPage;
